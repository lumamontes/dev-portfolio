---
interface Props {
  responseTimeLabel: string;
  responseTime: string;
  remoteWorldwideLabel: string;
}

const { responseTimeLabel, responseTime, remoteWorldwideLabel } = Astro.props;
---

<div class="flex items-center justify-center gap-6 text-sm">
  <div class="flex items-center gap-2">
    <span class="text-white">{responseTimeLabel}: {responseTime}</span>
  </div>
  <span class="text-zinc-600">•</span>
  <div class="flex items-center gap-2">
    <span class="text-zinc-400">{remoteWorldwideLabel}</span>
  </div>
</div> 