[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18"

# Build hooks for GitHub webhook integration
# This allows automatic rebuilds when TIL content is updated
[[build.processing]]
  skip_processing = false